<h1>Forgot your password?</h1>

<%= semantic_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= f.semantic_errors %>

  <%= f.inputs do %>
    <%= f.input :email %>
  <% end %>

  <%= f.actions do %>
    <%= f.action :submit, label: "Send me reset password instructions",
      wrapper_html: { class: "btn-default" } %>
  <% end %>
<% end %>
