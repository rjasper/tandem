<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">TandemStory</a>
    </div>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <%= link_to "Account", "#", class: "dropdown-toggle", data: { toggle: "dropdown" } %>
          <ul class="dropdown-menu">
            <%= content_tag :li, link_to("Settings", edit_user_registration_path) if user_signed_in? %>
            <%= content_tag :li, link_to("Sign in", new_user_session_path) unless user_signed_in? %>
            <%= content_tag :li, link_to("Sign up", new_user_registration_path) unless user_signed_in? %>
            <%= content_tag :li, link_to("Sign out", destroy_user_session_path, method: :delete) if user_signed_in? %>
            <%= content_tag :li, link_to("Forgot your password?", new_user_password_path) unless user_signed_in? %>
            <%= content_tag :li, link_to("Didn't receive confirmation instructions?", new_user_confirmation_path) unless user_signed_in? %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
